# Dicee
A simple iOS dice rolling app built with UIKit.  The app simulates rolling two dice and displays the result. Implemented programmatically using Auto Layout and UIStackView without Storyboards.  Includes basic UI composition, event handling, and dynamic image updates.
